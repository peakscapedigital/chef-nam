# Note: www → apex is handled by a zone-level Cloudflare Redirect Rule, NOT here.
# Cloudflare Workers validates _redirects strictly (absolute URLs / bad status
# codes are hard errors, unlike Pages which only warned), and per the site-deploy
# standard www handling lives at the zone, not in _redirects.

# Redirect old/non-existent blog posts to main blog page
/blog/corporate-event-catering /blog 301
/blog/seasonal-global-flavors /blog 301
/blog/memorable-wedding-menus /blog 301

# Redirect test pages to homepage
/test-services/ / 301
/colors-test/ / 301

# Cloudflare email protection
/cdn-cgi/l/email-protection /start-planning 301