{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "//": "chef-nam-catering is the Sanity Studio — a separate project with its own package.json + tsconfig. It is not part of the site build and is excluded so `astro check` only types the actual app.",
  "exclude": ["dist", "chef-nam-catering"]
}
